<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myfirstapp.finalproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/go_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/go_logo_round"
        android:supportsRtl="true"
        android:theme="@style/myStyle">
        <activity android:name=".Items.DetailsItem"></activity>
        <activity android:name=".AppPages.Beginning">

        </activity>
        <activity android:name=".Onboarding.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>